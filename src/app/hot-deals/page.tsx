import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "WashiCha | Buy Now",
    description: "Hot Deals Page of WashiCha",
};

const HotDeals = () => {
  return (
    <div>Hot Deals</div>
  )
}

export default HotDeals